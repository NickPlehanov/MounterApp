<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid"
             x:Class="MounterApp.Views.MountsPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackLayout Grid.Row="1" Grid.ColumnSpan="3">
            <Expander Margin="5">
                <Expander.Header>
                    <Label  Text="{Binding HeaderNotSended}" FontAttributes="Bold" Visual="Material"/>
                </Expander.Header>
                <!--<StackLayout>-->
                    <!--<Label Text="{Binding MsgNotSended}" IsVisible="{Binding MsgNotSendedVisibility}"/>-->
                    <!--<dg:DataGrid ItemsSource="{Binding NotSendedMounts}">
                        <dg:DataGrid.Columns>
                            <dg:DataGridColumn Width="*" Title="Объект" PropertyName=""/>
                        </dg:DataGrid.Columns>
                    </dg:DataGrid>-->
                    <CollectionView Grid.ColumnSpan="2" Grid.Row="1" Margin="5" 
                                ItemsSource="{Binding NotSendedMounts}" SelectionMode="Single" 
                                    SelectedItem="{Binding NotSendedMount}"
                                    SelectionChangedCommand="{Binding SelectMountCommand, Mode=TwoWay}">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" Span="4"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout>
                                    <!--<Image Source="{Binding Path=ImgSrc}" Margin="5"/>
                                    <Label Text="{Binding Path=_Types.PhotoTypeName}" VerticalTextAlignment="Center"/>-->
                                <Label Text="{Binding Path=ObjectNumber}" HeightRequest="100" WidthRequest="100" BackgroundColor="Azure" Margin="5" 
                                       HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                                <!--<Button Text="{Binding Path=ObjectNumber}"/>-->
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                <!--</StackLayout>-->
            </Expander>
        </StackLayout>
        <Button Grid.Row="0" Grid.Column="2" Text="+" FontSize="Large" Command="{Binding NewMountCommand}"/>
    </Grid>
</ContentPage>