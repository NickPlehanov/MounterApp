<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:datagrid="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid"
             x:Class="MounterApp.Views.NewMountpage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Label BackgroundColor="WhiteSmoke" Text="Информация об объекте" FontAttributes="Bold" HorizontalTextAlignment="Center" Margin="10" FontSize="24" />
                <StackLayout Margin="5">
                    <Label Text="№ объекта" HorizontalOptions="Center" FontAttributes="Bold"/>
                    <Entry Keyboard="Numeric" Text="{Binding ObjectNumber}"/>
                    <Label Text="Название объекта" HorizontalOptions="Center" FontAttributes="Bold"/>
                    <Entry Keyboard="Text" Text="{Binding ObjectName}"/>
                    <Label Text="Адрес объекта" HorizontalOptions="Center" FontAttributes="Bold"/>
                    <Entry Keyboard="Text" Text="{Binding ObjectAddress}"/>
                    <Label Text="Подъездные пути" HorizontalOptions="Center" FontAttributes="Bold"/>
                    <Entry Keyboard="Text" Text="{Binding ObjectDriveways}" />
                </StackLayout>
            </StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.ColumnSpan="2" Grid.Row="0" BackgroundColor="WhiteSmoke" Text="Фотографии объекта" FontSize="24" 
                       FontAttributes="Bold" HorizontalTextAlignment="Center" Margin="10"/>
                <StackLayout Grid.Row="1" Grid.Column="0" Margin="5">
                    <Picker Title="Выберите тип фото" ItemsSource="{Binding PhotoNames}" ItemDisplayBinding="{Binding PhotoTypeName}" SelectedItem="{Binding PhotoName}"/>
                    <Entry Text="{Binding PhotoComment}"/>
                    <Button Text="Добавить фото" Command="{Binding AddNewPhotoCommand}"/>             
                </StackLayout>
                <StackLayout Grid.Row="1" Grid.Column="1" Margin="5">
                    <Image x:Name="img" Source="{Binding ImgSrc}" Aspect="AspectFit" >
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding TakePhotoCommand}"/>
                        </Image.GestureRecognizers>
                    </Image>
                </StackLayout>
                <StackLayout Grid.ColumnSpan="2" Grid.Row="2" Margin="5">
                    <datagrid:DataGrid ItemsSource="{Binding Photos, Mode=TwoWay}" PullToRefreshCommand="{Binding RefreshCommand}" SelectedItem="{Binding SelectedPhoto}">
                        <datagrid:DataGrid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding DeleteCommand}"></TapGestureRecognizer>
                        </datagrid:DataGrid.GestureRecognizers>
                        <datagrid:DataGrid.Columns>
                            <datagrid:DataGridColumn Width="*" Title="Название" PropertyName="Path"/>
                        </datagrid:DataGrid.Columns>
                    </datagrid:DataGrid>
                </StackLayout>
            </Grid>
            <Button Text="Отправить данные" Command="{Binding SendToServer}" Margin="5"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>